# 接口方法修复说明

## 🐛 问题

用户访问 `http://localhost:8080/v1/task/list` 时返回错误：
```json
{
  "meta": {
    "statusCode": 9999,
    "statusMsg": "系统异常: Request method 'GET' is not supported"
  },
  "data": null
}
```

## 🔍 原因分析

1. **SpringBoot 后端定义**: `/v1/task/list` 接口只支持 `POST` 方法
2. **浏览器直接访问**: 浏览器默认使用 `GET` 方法
3. **前端代码**: 正确使用了 `POST` 方法（`http.post()`）

## ✅ 解决方案

### 1. 添加 GET 方法支持

为了兼容浏览器直接访问和测试，添加了 `GET` 方法支持：

**文件**: `agent-api/src/main/java/com/studyagent/api/controller/TaskController.java`

**修改内容**:
- 保留原有的 `@PostMapping("/list")` 方法（用于前端调用）
- 新增 `@GetMapping("/list")` 方法（用于浏览器直接访问和测试）
- GET 方法使用查询参数（Query Parameters）
- POST 方法使用请求体（Request Body）

### 2. 修复 POST 方法

- 将 `@RequestBody` 改为 `@RequestBody(required = false)`，允许空请求体
- 添加空请求体的默认值处理

## 📝 使用方式

### POST 方法（前端调用）

```bash
curl -X POST "http://localhost:8080/v1/task/list" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <token>" \
  -d '{
    "task_keyword": "搜索关键词",
    "task_status": 0,
    "order": 1,
    "page_no": 1,
    "page_size": 10
  }'
```

### GET 方法（浏览器访问或测试）

```bash
# 使用查询参数
curl "http://localhost:8080/v1/task/list?page_no=1&page_size=10&task_status=0"

# 浏览器直接访问
http://localhost:8080/v1/task/list
http://localhost:8080/v1/task/list?page_no=1&page_size=10
```

## 🎯 查询参数说明

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| `task_keyword` | String | 否 | - | 搜索关键词 |
| `task_status` | Integer | 否 | - | 任务状态（0-全部, 1-草稿, 2-待执行, 3-执行中, 4-已完成, 5-失败） |
| `order` | Integer | 否 | - | 排序方式（1-最新优先, 2-最旧优先, 3-标题A-Z, 4-标题Z-A, 5-最高质量优先） |
| `page_no` | Integer | 否 | 1 | 页码 |
| `page_size` | Integer | 否 | 10 | 每页数量 |

## ✅ 验证

### 1. 浏览器直接访问

访问 `http://localhost:8080/v1/task/list`，应该返回：
```json
{
  "meta": {
    "statusCode": 0,
    "statusMsg": "Success"
  },
  "data": {
    "task_list": [],
    "total": 0,
    "page_no": 1,
    "page_size": 10
  }
}
```

### 2. 前端调用

前端代码无需修改，继续使用 `POST` 方法：
```typescript
const response = await taskService.getList({
  page_no: 1,
  page_size: 10
});
```

## 📊 接口兼容性

| 方法 | 用途 | 参数位置 | 推荐使用场景 |
|------|------|----------|-------------|
| POST | 前端调用 | Request Body | 生产环境，传递复杂查询条件 |
| GET | 浏览器访问/测试 | Query Parameters | 开发测试，简单查询 |

## ⚠️ 注意事项

1. **前端代码**: 无需修改，继续使用 `POST` 方法
2. **浏览器访问**: 现在可以直接访问，使用查询参数
3. **默认值**: GET 方法如果不传参数，使用默认值（page_no=1, page_size=10）
4. **认证**: 两种方法都支持可选的 `Authorization` 头

