<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.studyagent</groupId>
    <artifactId>studyagent-backend</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>StudyAgent Backend</name>
    <description>StudyAgent Backend Parent POM</description>

    <modules>
        <module>agent-common</module>
        <module>agent-api</module>
        <module>agent-service</module>
        <module>agent-infra</module>
        <module>agent-start</module>
    </modules>

    <properties>
        <java.version>17</java.version>
        <maven.compiler.source>17</maven.compiler.source>
        <maven.compiler.target>17</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        
        <!-- Spring Boot（3.4.x：兼容 Spring AI 1.0.x 稳定版；原 3.1.5 与 Spring AI 全部版本不兼容） -->
        <spring-boot.version>3.4.13</spring-boot.version>
        
        <!-- MyBatis Plus (3.5.12：适配 Spring Boot 3.4；原 3.5.5 的 MapperFactoryBean 与 Boot 3.4 不兼容) -->
        <mybatis-plus.version>3.5.12</mybatis-plus.version>
        
        <!-- MySQL -->
        <mysql.version>8.0.33</mysql.version>
        
        <!-- Stripe -->
        <stripe.version>24.0.0</stripe.version>
        
        <!-- Clerk SDK -->
        <clerk.version>4.1.2</clerk.version>
        
        <!-- Swagger -->
        <swagger.version>2.2.0</swagger.version>
        
        <!-- Lombok -->
        <lombok.version>1.18.40</lombok.version>
        
        <!-- Commons -->
        <commons-lang3.version>3.13.0</commons-lang3.version>
        <gson.version>2.10.1</gson.version>

        <!-- PostHog 官方 Server SDK -->
        <posthog.version>2.7.0</posthog.version>

        <!-- Caffeine：Verla 上下文查询本地缓存（PR-10） -->
        <caffeine.version>3.1.8</caffeine.version>

        <!-- Sqids：对外 ID 混淆编码（V1 taskId + V2 public id） -->
        <sqids.version>0.1.0</sqids.version>

        <!-- Spring AI Alibaba（Learning Canvas demo 的 LLM / Agent 底座；1.1.2.0 配 spring-ai 1.1.2，兼容 Boot 3.4） -->
        <spring-ai-alibaba.version>1.1.2.0</spring-ai-alibaba.version>
        <spring-ai.version>1.1.2</spring-ai.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Spring Boot BOM -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            
            <!-- MyBatis Plus -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-boot-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>

            <!-- MyBatis Plus Boot 3.x Starter（Spring Boot 3.4 用；Learning Canvas demo 的 agent-infra 依赖） -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>

            <!-- MyBatis Plus JSqlParser（3.5.9+ 分页插件所在模块） -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-jsqlparser</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            
            <!-- MyBatis Plus Extension (显式声明，包含插件类) -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-extension</artifactId>
                <version>${mybatis-plus.version}</version>
            </dependency>
            
            <!-- MySQL -->
            <dependency>
                <groupId>mysql</groupId>
                <artifactId>mysql-connector-java</artifactId>
                <version>${mysql.version}</version>
            </dependency>
            
            <!-- Stripe -->
            <dependency>
                <groupId>com.stripe</groupId>
                <artifactId>stripe-java</artifactId>
                <version>${stripe.version}</version>
            </dependency>
            
            <!-- Clerk SDK (官方 Java SDK) -->
            <dependency>
                <groupId>com.clerk</groupId>
                <artifactId>backend-api</artifactId>
                <version>${clerk.version}</version>
            </dependency>
            
            <!-- Swagger -->
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${swagger.version}</version>
            </dependency>
            
            <!-- Lombok -->
            <dependency>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
            </dependency>
            
            <!-- Commons -->
            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-lang3</artifactId>
                <version>${commons-lang3.version}</version>
            </dependency>
            
            <dependency>
                <groupId>com.google.code.gson</groupId>
                <artifactId>gson</artifactId>
                <version>${gson.version}</version>
            </dependency>

            <!-- PostHog 官方 Server SDK -->
            <dependency>
                <groupId>com.posthog</groupId>
                <artifactId>posthog-server</artifactId>
                <version>${posthog.version}</version>
            </dependency>

            <!-- Caffeine: 本地高性能缓存，用于 Verla 上下文查询的 lookaside -->
            <dependency>
                <groupId>com.github.ben-manes.caffeine</groupId>
                <artifactId>caffeine</artifactId>
                <version>${caffeine.version}</version>
            </dependency>

            <!-- Sqids: 将数字 ID 编码为短字符串，避免对外暴露自增 ID -->
            <dependency>
                <groupId>org.sqids</groupId>
                <artifactId>sqids</artifactId>
                <version>${sqids.version}</version>
            </dependency>

            <!-- Spring AI Alibaba Agent Framework（Learning Canvas demo 编排底座） -->
            <dependency>
                <groupId>com.alibaba.cloud.ai</groupId>
                <artifactId>spring-ai-alibaba-agent-framework</artifactId>
                <version>${spring-ai-alibaba.version}</version>
            </dependency>

            <!-- Spring AI OpenAI Starter: OpenAI-compatible ChatModel provider -->
            <dependency>
                <groupId>org.springframework.ai</groupId>
                <artifactId>spring-ai-starter-model-openai</artifactId>
                <version>${spring-ai.version}</version>
            </dependency>

            <!-- Agent Common (内部模块) -->
            <dependency>
                <groupId>com.studyagent</groupId>
                <artifactId>agent-common</artifactId>
                <version>${project.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <!-- 显式声明 Maven Central，避免仅用阿里云镜像时 posthog 等依赖解析失败 -->
    <repositories>
        <repository>
            <id>central</id>
            <name>Maven Central</name>
            <url>https://repo.maven.apache.org/maven2</url>
            <releases><enabled>true</enabled></releases>
            <snapshots><enabled>false</enabled></snapshots>
        </repository>
    </repositories>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring-boot.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.2.5</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.13.0</version>
                    <configuration>
                        <parameters>true</parameters>
                        <annotationProcessorPaths>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                                <version>${lombok.version}</version>
                            </path>
                        </annotationProcessorPaths>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
