# 快速启动测试指南

## ✅ 当前状态

项目已经可以启动，但**部分接口需要配置才能正常使用**。

## 🚀 立即测试（最小配置）

### 1. 启动应用（不需要数据库）

```bash
cd studyagent-backend
mvn clean install -DskipTests
cd agent-start
mvn spring-boot:run
```

### 2. 测试健康检查接口（100% 可以工作）

```bash
curl http://localhost:8080/health
```

**预期响应**:
```json
{
  "meta": {
    "statusCode": 0,
    "statusMsg": "success"
  },
  "data": {
    "status": "healthy",
    "app_name": "StudyAgent Backend",
    "environment": "development",
    "database": "not_configured"  // 或 "connected" 如果配置了数据库
  }
}
```

### 3. 访问 Swagger UI（100% 可以工作）

浏览器访问: http://localhost:8080/swagger-ui.html

可以看到所有接口的文档。

## ⚠️ 需要配置才能使用的接口

### 需要数据库配置

以下接口需要数据库连接：

- POST `/v1/file/upload` - 文件上传
- POST `/v1/file/export` - 文件导出
- POST `/v1/task/list` - 任务列表
- POST `/v1/task/detail` - 任务详情
- POST `/v1/task/submit` - 提交任务
- POST `/v1/task/rate` - 任务评价
- GET `/v1/download/output/{taskId}/{filename}` - 下载文件
- GET `/v1/editor/content/{taskId}` - 获取编辑器内容
- PUT `/v1/editor/content/{taskId}` - 保存编辑器内容

**配置步骤**:
1. 确保 MySQL 运行
2. 创建数据库: `CREATE DATABASE studyagent;`
3. 执行初始化 SQL: `mysql -u root -p studyagent < docker/init.sql`
4. 修改 `application.yml` 中的数据库用户名密码

### 需要 Clerk 配置

以下接口需要 Clerk Secret Key：

- GET `/v1/auth/me` - 获取用户信息
- POST `/v1/task/submit` - 提交任务（需要登录）

**配置步骤**:
1. 在 `application.yml` 中配置 `clerk.secret-key`
2. 或设置环境变量 `CLERK_SECRET_KEY`

**临时解决方案**: 修改 `AuthInterceptor`，暂时允许未认证访问（仅用于测试）

### 需要 Stripe 配置

以下接口需要 Stripe Secret Key：

- POST `/v1/payment/create-checkout-session`
- GET `/v1/payment/session-status`
- POST `/v1/webhook/stripe`

**注意**: GET `/v1/payment/config` 可以调用，但返回的配置可能不完整。

### 需要 Python 后端

以下功能需要 Python 后端运行：

- POST `/v1/task/submit` - 提交任务后会调用 Python 后端执行

**临时解决方案**: 可以暂时注释掉 `TaskApplicationService` 中的 Python 后端调用

## 📊 接口可用性矩阵

| 接口 | 不需要配置 | 需要数据库 | 需要Clerk | 需要Stripe | 需要Python后端 |
|------|-----------|-----------|-----------|-----------|----------------|
| GET `/health` | ✅ | ❌ | ❌ | ❌ | ❌ |
| GET `/swagger-ui.html` | ✅ | ❌ | ❌ | ❌ | ❌ |
| GET `/v1/auth/me` | ❌ | ✅ | ✅ | ❌ | ❌ |
| POST `/v1/file/upload` | ❌ | ✅ | ❌ | ❌ | ❌ |
| POST `/v1/file/export` | ❌ | ✅ | ❌ | ❌ | ❌ |
| POST `/v1/task/list` | ❌ | ✅ | ⚠️可选 | ❌ | ❌ |
| POST `/v1/task/detail` | ❌ | ✅ | ⚠️可选 | ❌ | ❌ |
| POST `/v1/task/submit` | ❌ | ✅ | ✅ | ❌ | ✅ |
| POST `/v1/task/rate` | ❌ | ✅ | ❌ | ❌ | ❌ |
| GET `/v1/download/output/{id}/{file}` | ❌ | ✅ | ❌ | ❌ | ❌ |
| GET `/v1/editor/content/{id}` | ❌ | ✅ | ❌ | ❌ | ❌ |
| PUT `/v1/editor/content/{id}` | ❌ | ✅ | ❌ | ❌ | ❌ |
| POST `/v1/payment/create-checkout-session` | ❌ | ❌ | ❌ | ✅ | ❌ |
| GET `/v1/payment/config` | ✅ | ❌ | ❌ | ⚠️部分 | ❌ |

## 🎯 推荐测试顺序

### 第1步：基础启动测试（无需配置）

```bash
# 1. 启动应用
cd agent-start
mvn spring-boot:run

# 2. 测试健康检查
curl http://localhost:8080/health

# 3. 访问 Swagger UI
# 浏览器打开: http://localhost:8080/swagger-ui.html
```

**预期**: 应用启动成功，健康检查返回成功，Swagger UI 可以访问。

### 第2步：数据库配置测试

```bash
# 1. 配置数据库（修改 application.yml）
# 2. 重启应用
# 3. 测试任务列表（不需要登录）
curl -X POST "http://localhost:8080/v1/task/list" \
  -H "Content-Type: application/json" \
  -d '{"page_no": 1, "page_size": 10}'
```

**预期**: 返回任务列表（可能为空数组 `[]`）。

### 第3步：文件上传测试

```bash
# 测试文件上传
curl -X POST "http://localhost:8080/v1/file/upload" \
  -H "Content-Type: application/json" \
  -d '{"raw_body": "SGVsbG8gV29ybGQK"}'
```

**预期**: 返回 `object_id`。

### 第4步：完整功能测试

配置 Clerk、Stripe 和 Python 后端后，测试完整流程。

## ⚠️ 已知问题

1. **文件上传**: `FileController.uploadFile` 中文件名是硬编码的，需要从请求中获取
2. **Clerk 集成**: 当前使用 HTTP 调用，建议使用 Clerk Java SDK
3. **时间字段**: 目前手动设置，可以配置 MyBatis-Plus 自动填充
4. **任务列表分页**: 当前实现较简单，需要完善分页逻辑

## 🔧 快速修复建议

如果遇到问题，可以：

1. **跳过认证**: 修改 `AuthInterceptor`，返回 `true`
2. **跳过 Python 后端**: 注释掉 `TaskApplicationService` 中的调用
3. **使用内存数据库**: 暂时使用 H2 数据库进行测试

## ✅ 总结

**可以立即启动和测试的接口**:
- ✅ GET `/health` - 健康检查
- ✅ GET `/swagger-ui.html` - API 文档
- ✅ GET `/v1/payment/config` - 支付配置（部分功能）

**需要数据库配置的接口**:
- ⚠️ 所有数据相关的接口

**需要外部服务配置的接口**:
- ⚠️ 认证相关（Clerk）
- ⚠️ 支付相关（Stripe）
- ⚠️ 任务执行（Python后端）

**建议**: 先测试健康检查和 Swagger UI，确认应用可以正常启动，然后再逐步配置数据库和外部服务。

